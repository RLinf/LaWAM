# 打乱 z 是什么意思：LaWAM-VGGT Stage 1 实验结果全面分析

**日期**：2026-08-24
**对象**：`/home/ma-user/work/lam_runs/vggt_vae_libero/checkpoints/epoch=39.ckpt`
**评估脚本**：`tools/eval_lam_probes.py`（主探针）、`tools/eval_lam_controls.py`（本文新增的对照）

---

## 0. 先纠正我上一轮的一个错误结论

上一轮我说过：

> mse_shuffled 0.5408 ≈ identity 0.5496 —— **模型比"抄袭"多出来的全部本事，100% 来自 z**

**这句话是错的。** 它建立在一个未经检验的假设上：打乱 z 之后的模型 ≈ 一个没有 z 的模型。这个假设不成立——本文第 4 节的 `z=0` 对照给出了真实的分解：z 贡献的是 **77%**，不是 100%。

之所以会错，是因为"打乱"和"置零"是两件不同的事，而我当时只做了前者。这恰好是本文要说清楚的核心问题。

---

## 1. "打乱 z" 到底在做什么

### 1.1 机制

模型训练完之后，一次正常的预测长这样：

```
样本 A:   u_t^A  (A 的当前画面特征)  +  z^A  (从 A 的前后两帧推出的动作)  →  û^A
样本 B:   u_t^B                     +  z^B                              →  û^B
```

"打乱 z" 就是**把 z 在 batch 内部重新配对，其他一切不动**：

```
样本 A:   u_t^A  +  z^B   →  û'^A       ← 用 B 的动作去驱动 A 的画面
样本 B:   u_t^B  +  z^C   →  û'^B
                   ↑
              只有这一项换了
```

然后拿 `û'^A` 去和 A 的**真实**未来 `u_{t+τ}^A` 算 MSE。

代码上就是重跑一次解码器，输入特征原封不动，latent 换成一个随机排列（`eval_lam_probes.py:189-199`）：

```python
perm = torch.randperm(N)
z_shuf = Z[perm[s:e]]                      # 唯一的改动
r = model.decoder(dec_in[s:e], z_shuf)     # dec_in 不变
```

### 1.2 这是在问什么

用大白话讲，是在问：**"给它一个错的动作指令，它的预测会不会变差？"**

- 如果**会明显变差** → 说明解码器真的在读 z、真的把 z 当动作指令用。z 是活的。
- 如果**几乎不变** → 说明解码器压根没理 z，它只是从当前画面外推。**z 是个摆设。**

定义：

```
delta_z  =  mse(用打乱的 z)  −  mse(用正确的 z)
```

`delta_z` 越大，说明 z 越重要。

### 1.3 为什么必须做这个检验

这是整个 Stage 1 里**唯一能证伪的地方**。训练 loss 降到 0.056、cosine 0.992，这些数字**完全不能说明 z 有用**，因为模型有一条现成的作弊路径：

> 解码器完全忽略 z，只学"当前画面 → 未来画面"的平均演化。

这条路径的 loss 也会降得很好看。而一旦它走了这条路，**Stage 2 直接报废**——Stage 2 的全部内容就是训一个 VLM 去输出 z，如果 z 不控制任何东西，策略输出什么都无所谓，等于什么都没学。

所以 `delta_z` 不是"锦上添花的额外指标"，它是**决定这个项目是否继续的开关**。

### 1.4 一个更贴切的类比

想象你训练一个人做菜，只给他看"食材照片"和"成品照片"，让他推断中间的烹饪步骤（z），再让另一个人拿着食材照片 + 这份步骤去还原成品。

打乱 z，就是**把 A 菜的步骤说明书发给做 B 菜的人**。

- 如果他照做后端出来的东西明显不对 → 说明他真的在看说明书。
- 如果他端出来的还是那道 B 菜 → 说明他根本没看说明书，全靠食材猜的。**那这份说明书就毫无价值。**

---

## 2. 主结果

**评估设定**：960 个样本，来自 **84 个训练时完全没见过的 episode**（1693 个中按 episode 尾部切 5%）。关增强、`z=μ`（VAE 取均值不采样）、全部指标在**标准化特征空间**计算。

> **为什么必须标准化**：VGGT 没有最后一层 LayerNorm，输出的是 raw 残差流。实测每个 token 有约 75% 是所有 token 共享的同一个常量（‖平均 token‖=76.4 vs 平均残差范数 43.5），per-channel std 跨度 78 倍。直接算余弦会**秩反转**——不相干的跨 episode 样本对（0.871）反而比同一条轨迹的时序对（0.853）更"像"。不标准化的数字是**主动误导**，不是不够精确。

### 2.1 基线对比

| | MSE | 含义 |
|---|---|---|
| 预测数据集均值 | **1.0000** | 标准化的定义，参考零点 |
| 抄袭当前帧 `û = u_t` | **0.5566** | 作弊路径的分数 |
| **本模型** | **0.1592** | ✅ 比抄袭好 3.5 倍 |

1.6 秒里画面大部分不变，所以"抄袭"是个相当强的基线（0.557 < 1.0）。模型压到 0.159，说明它确实在预测**变化**，不是在复读输入。

### 2.2 中心化余弦（论文 Fig.10 三条曲线）

| | 值 |
|---|---|
| `cos(预测, 真实未来)` | **0.9189** |
| `cos(当前, 真实未来)` | 0.7377 |
| `cos(预测, 当前)` | 0.7607 |

健康形态：预测比当前帧更接近真实未来（0.919 > 0.738），且预测**没有**塌回当前帧（0.761 没有反超 0.919）。

---

## 3. 打乱 z 的结果

```
mse_pred      = 0.1592     ← 正确的 z
mse_shuffled  = 0.5441     ← 打乱的 z
delta_z       = 0.3850     ← 目标 > 0.1，实际超出 3.9 倍
```

给一个错的动作指令，误差涨 **242%**。z 是活的，而且是**强烈**地活着。

---

## 4. 新增的对照实验（这次补的）

`delta_z = 0.385` 和 `R² = 0.51` 单独看都不可解释——它们缺对照。补了两组。

### 4.1 对照 B：四种 z 替换方式

打乱 z 这个操作同时改变了两件事：**动作错了**，以及**场景/任务也错了**（LIBERO 四个 suite，A 样本可能是"把碗放进抽屉"，B 样本是"关柜门"）。一个只编码了"这是哪个 suite"的 z 也能造出很大的 `delta_z`。所以要拆开。

| 替换方式 | MSE | delta_z | 与 u_t 的距离 |
|---|---|---|---|
| 真实 z | 0.1592 | 0 | 0.4762 |
| **跨全集打乱 z** | 0.5441 | **0.3850** | 0.5125 |
| **同 episode 内打乱 z** | 0.4742 | **0.3151** | 0.4717 |
| z = 0 | 0.4642 | 0.3051 | 0.3332 |
| z ~ 高斯噪声 | 0.4862 | 0.3270 | 0.3836 |
| 抄袭 u_t（identity） | 0.5566 | 0.3974 | 0 |

**读法一：同 episode 内打乱，保留了 82% 的效应（0.315 / 0.385）。**
同一条轨迹内换 z，场景、物体、任务、光照全部相同，**只有运动阶段不同**（比如"正在伸手"换成"正在回撤"）。误差仍然从 0.159 涨到 0.474。→ **z 编码的是运动本身，不是"这是哪个任务"这种粗粒度标签。** 这是本次对照最重要的一条。

**读法二：`z=0` 时解码器没有塌成抄袭。**
看最后一列——`z=0` 时预测与 `u_t` 的距离是 0.3332，**不是 0**。说明没有 z 时，解码器仍会预测出"某种平均运动"（一个场景条件下的运动先验），只是不知道具体是哪一种。

由此得到**诚实的三段分解**：

```
抄袭输入          0.5566
   ↓  −0.0924    ← 场景条件下的平均运动先验（不需要 z）
z=0 的预测        0.4642
   ↓  −0.3050    ← z 提供的具体动作信息
真实 z 的预测      0.1592
```

**z 贡献 77%，运动先验贡献 23%。** 这就是第 0 节纠正的那个数字——我之前说 100% 是因为只看了"打乱"没看"置零"。

**读法三：错的 z 比没有 z 更糟（0.5441 > 0.4642）。**
这条容易被忽略但很有说服力。如果 z 只是个"开关"或弱调制，给错的 z 顶多退化到 z=0 的水平。但给错的 z 反而**更差**，说明解码器把 z 当作一个**有方向的指令**在积分——它自信地朝错误方向预测了。这是 z 被深度整合进解码器的证据，而不是被当作可有可无的旁路。

### 4.2 对照 A：z 和当前帧，谁能解出动作

`R² = 0.51` 缺的对照是：**当前帧本身能解出多少动作？** LIBERO 里机械臂的位置本来就强烈暗示它下一步要干什么。如果 `u_t` 的 32 维 PCA 也能拿 0.51，那 z 就没有任何额外贡献。

设定：ridge 回归 → 未来 32 帧动作块（每 4 帧采一个 → 8×7=56 维），**按 episode 切分** train/test（84 episodes → 480/260 样本），PCA **只在 train episode 上拟合**避免泄漏，**维度严格对齐 32 vs 32** 保证比的是内容而非容量。

| 输入 | R² |
|---|---|
| **z（32 维，本模型的 latent action）** | **0.5088** |
| PCA32(u_t 的 token 均值)｜对照 | 0.2441 |
| PCA32(u_t 全 token)｜对照 | 0.2981 |
| z + PCA32(u_t) 拼接 | 0.5707 |

**增量 = 0.5088 − 0.2981 = +0.2106。**

三点结论：

1. 当前帧确实能解出一部分动作（0.298）——所以不做这个对照，0.51 是没法解释的。
2. **z 显著超出当前帧（+0.21）**，说明 z 携带了当前帧里没有的信息——即"接下来实际发生了什么"，这只可能来自未来帧 `o_{t+τ}`，正是 IDM 该干的事。
3. 拼接后（0.571）比 z 单独（0.509）还高一点，说明 z 也**没有**完全包含 `u_t` 里的信息。这符合预期：z 只有 32 维，且双流增强主动阻止它携带外观/位置信息。

---

## 5. 空间证据：z 具体控制画面的哪些部分

`tools/viz_vggt_lam.py` 把上面的标量指标做成了空间图。对每个图块（16×16 网格）分别计算：

- **真实变化**：`‖u_{t+τ} − u_t‖`，哪里真的动了
- **z 敏感度**：`‖û(z) − û(z_打乱)‖`，换个 z 会让哪里的预测改变

两者的相关系数：

```
corr(真实变化, z敏感度) = 0.580
```

**z 不是均匀地扰动整幅画面，而是专门作用在真正发生运动的区域**（机械臂、被操作的物体）。这是 `delta_z=0.385` 的空间版本——如果 z 只是个全局偏置，这个相关会接近 0。

图：`/home/ma-user/work/lam_runs/viz/z_sensitivity.png`

---

## 6. 为什么这个架构会逼出 latent action

回到机制本身。训练时**只有一个损失**：预测的未来特征要接近真实的未来特征。**全程没有任何动作标签。** 那 z 为什么会变成动作？因为两条约束把它逼到了那个位置。

```
        aug_A(o_t), aug_A(o_{t+1.6s})  ─┐
                                        ├─→ VGGT-1B (冻结) ─→ [B,4,256,2048]
        aug_B(o_t), aug_B(o_{t+1.6s})  ─┘        │
                    ┌───────────────────────────┼──────────────┐
              enc_in (aug_A 两帧)          dec_in (aug_B 的 t)  tgt (aug_B 的 t+τ)
                    │                            │              │
              IDM 编码器 (24层)                   │              │
                    │                            │              │
                 z [32 维]  ← 瓶颈               │              │
                    └── AdaLN 调制 → 世界模型解码器(12层) → û ──── smooth_L1
```

**约束一：瓶颈。** 目标是 256 token × 2048 维 ≈ 52 万个数。z 只有 **32 个数**，压缩比约 16000:1。z 装不下一张图，只装得下"发生了什么变化"。

**约束二：双流增强（关键机制，不只是正则）。** IDM 看的两帧用 `aug_A`，解码器看的两帧用 `aug_B`，两套裁剪+颜色扰动**独立随机**，但各自在时间上一致。所以 z 就算想携带"物体在左上角、偏红"，传到解码器那边坐标和色调全变了，对不上号。**只有跟外观无关的运动信息能穿过瓶颈。**

训完之后 IDM 扔掉，留下的是解码器和一套 z 的语义。Stage 2 训 VLM 去输出 z——相当于让它说"往右前方伸过去抓一下"，而不必学具体关节角，于是不同机器人的数据可以共享同一套 z。

**本次复现改的一处**：论文用 DINOv3（语义空间："这是个杯子"），我们换成 VGGT-1B（3D 几何空间："这个平面离相机 40cm"）。所以本实验回答的是：**世界模型建在几何特征空间上，latent action 学不学得出来？**

### 6.1 一个承重的实现细节

VGGT 的 aggregator 是 frame-attention 和 global-attention 交替的，**global-attention 会把 S 维上的所有帧混在一起**。如果把 `(o_t, o_{t+τ})` 当成 S=2 送进去，`t` 时刻的特征里就会混进未来的信息——**未来泄漏，任务当场作废**，而且 loss 会降得格外好看，从指标上看不出来。

实现上是把 `(B,T)` 压平进 batch 维、以 `S=1` 逐帧调用。写了回归测试实测确认：

```
batch 维堆叠:  max|Δ| = 0.0     ← 逐位相同，安全
S    维堆叠:  max|Δ| = 20.3    ← 确实泄漏
```

测试留在 `tools/check_vggt_encoder.py`，防止后人为了提速把它"优化"成一次 S=4 的调用。

---

## 7. 判据核对

| 判据 | 目标 | 实测 | |
|---|---|---|---|
| 打败所有平凡基线 | `mse_pred < min(identity, mean)` | 0.159 < 0.557 < 1.0 | ✅ |
| `delta_z`（跨全集） | > 0.1 | **0.385** | ✅ 3.9× |
| `delta_z`（同 episode 内） | 无预设，越接近前者越好 | **0.315**（保留 82%） | ✅ |
| `z2action_r2` | > 0.3 | **0.509** | ✅ |
| z 相对 u_t 的增量 R² | 需 > 0 | **+0.211** | ✅ |
| 训练/验证 gap | < 2× | 0.0419 / 0.0561 = **1.34×** | ✅ |
| 空间相关 | 需 > 0 | **0.580** | ✅ |

**结论：VGGT 版 LaWAM Stage 1 复现成功。在 3D 几何特征空间上，latent action 学得出来，且质量足以支撑 Stage 2。**

---

## 8. 必须声明的局限

这些不是免责声明，是会影响结论解读的实质性限制：

1. **样本量小。** 960 个样本 / 84 个 episode。不同抽样下 `mse_pred` 在 0.1586–0.1592、`R²` 在 0.509–0.552 之间浮动（40 batch vs 60 batch 两次运行）。**结论的方向稳健，第三位小数不要当真。**

2. **只在 LIBERO 上评估。** 单一本体（Franka）、单一仿真环境、273K 帧。论文用了 3000 小时真机 + 1500 小时人类视频，数据量差约 700 倍。**论文 Fig.5 的跨本体泛化，本复现完全没有验证，也不具备验证条件。**

3. **`z2action_r2` 是必要条件不是充分条件。** 线性可解码说明 z 里有动作信息且组织规整。但 Stage 2 是反向的——从语言指令**生成** z。生成比解码难得多，R²=0.51 不保证 Stage 2 成功。

4. **`delta_z` 在留出 episode 上算，但 latent 由 IDM 从真实未来帧推出。** 也就是说 z 是"有上帝视角"的。这是 Stage 1 的定义使然（IDM 本来就看两帧），但意味着**这个数字衡量的是"z 空间是否有用"，不是"策略能不能推断出 z"**。后者是 Stage 2 的问题。

5. **`z = μ`，没有采样。** β=1e-5 的 KL 权重对连续高斯几乎没有约束，`z` 的后验可能非常尖。没有测 KL 塌缩程度（per-dim 的 active_dims），**32 维里可能有相当一部分是死的**。这项没做，应该补。

6. **VGGT vs DINOv3 没有做同条件对比。** 本次只跑了 VGGT 一路，没有在完全相同的数据/超参下跑一遍 DINOv3 基线。所以**不能说"VGGT 比 DINOv3 好"**，只能说"VGGT 上这套方法能跑通，且指标健康"。这是本复现最大的空白，也是最值得补的一个实验。

---

## 9. 建议的下一步

按性价比排序：

1. **测 `z` 的 active_dims**（半小时）。每维的 KL，看 32 维里实际用了几维。若只有 5-6 维活着，说明瓶颈可以更小，也说明 Stage 2 的目标空间比想象中窄。这是第 8 节第 5 条的直接补救。
2. **DINOv3 同条件基线**（约 2 天 GPU）。补上第 8 节第 6 条的空白，才谈得上"几何 vs 语义哪个更适合机器人世界模型"——这本来就是本复现想回答的问题。
3. **关增强的消融**。预期 `delta_z` 上升但 `z2action_r2` **下降**（z 开始携带外观信息）。这是对"双流增强是防泄漏机制"这一主张最锋利的检验。
4. Stage 2。

---

## 附：复现命令

```bash
cd /home/ma-user/work/dataset/xxd-dataset/dataset_yhw/WAM/LaWAM_official

# 主探针
PYTHONDONTWRITEBYTECODE=1 WANDB_MODE=disabled CUDA_VISIBLE_DEVICES=0 \
  python tools/eval_lam_probes.py

# 本文新增的对照
PYTHONDONTWRITEBYTECODE=1 WANDB_MODE=disabled CUDA_VISIBLE_DEVICES=0 \
  python tools/eval_lam_controls.py

# 可视化
PYTHONDONTWRITEBYTECODE=1 WANDB_MODE=disabled CUDA_VISIBLE_DEVICES=0 \
  python tools/viz_vggt_lam.py
```

日志里的 `OSError: [Errno 16] Device or resource busy: '.nfs...'` 和
`PermissionError: Operation not permitted` 是 dataloader worker 退出时
`multiprocessing` 清理 root-squash NFS 临时目录产生的已知无害噪音，不影响结果。
